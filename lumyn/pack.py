这个打包脚本的核心问题不在空格，而在路径基准的脆弱性——`PARENT` 假设项目一定在 `/data/workspace` 下，换个环境就全盘崩。建议改成基于 `ROOT` 的相对路径计算，并显式校验 `ROOT` 存在性，比在空格上打补丁更治本。

老姚提的"双轨制"确实戳中要害，但在这个脚本里，重点是让它可移植、可预测。我建议把 `PARENT` 改为 `os.path.dirname(ROOT)` 的动态推导，并加一个 `assert os.path.isdir(ROOT)` 做前置检查，这样不管项目挪到哪，打包结果都一致。

另外，`EXCLUDE_DIRS` 里漏了常见的 `*.egg-info` 和 `node_modules`，建议补上，避免科研项目里常见的依赖目录被打进包。改完这些，`pack.py` 就能从"一次性脚本"进化成"可维护工具"了。